<project name="Clover Report" default="current">

    <clover-format id="clover.format" type="${type}" orderBy="${orderBy}" filter="${filter}"/> 

    <clover-setup initString="${cloverdb}"/>

    <target name="historical">
        <clover-report>
            <current outfile="${output}"  summary="${summary}" charset="${charset}" title="${title}">
                <format refid="clover.format"/>
                <testsources dir="${tests}"/>
            </current>
            <historical outfile="${historyout}" historydir="${history}" charset="${charset}" title="${title}">
                <format refid="clover.format"/>
            </historical>
        </clover-report>
    </target>

    <target name="current">
        <clover-report>
            <current outfile="${output}" title="${title}" summary="${summary}" charset="${charset}">
                <format refid="clover.format"/>
                <testsources dir="${tests}"/>
            </current>
        </clover-report>
    </target>
    
</project>